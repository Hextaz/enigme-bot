app = "enigme-bot"
primary_region = "cdg" # Paris (Charles de Gaulle)

[build]
  builder = "paketobuildpacks/builder:base"

[env]
  PORT = "8080"

[mounts]
  source = "enigme_data"
  destination = "/app/data"

[processes]
  app = "npm start"
